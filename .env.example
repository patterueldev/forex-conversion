# Unirate API Key for currency conversion rates
UNIRATE_API_KEY=your_api_key_here

# Optional: CORS configuration for the server (comma-separated)
# Defaults: ALLOWED_HOSTS=localhost,thursday.local ALLOWED_PORTS=80,5173
# ALLOWED_HOSTS=localhost,thursday.local,saturday.local
# ALLOWED_PORTS=80,8080,5173
